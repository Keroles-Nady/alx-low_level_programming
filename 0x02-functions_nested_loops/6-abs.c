#include "main.h"

/**
 * main _abs - computes the absolute value of an integer
 * @n: The number in question.
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
